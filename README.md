Task 7 - SQL Joins and Aggregation

Objective: Perform SQL operations using the Customers and Orders tables to understand different types of joins and aggregation functions.

SQL Concepts Covered

CREATE DATABASE
CREATE TABLE
INSERT INTO
SELECT
INNER JOIN
LEFT JOIN
RIGHT JOIN
UNION
GROUP BY
COUNT()
Handling Unmatched Records

1.INNER JOIN  : Displays only customers who have placed orders.

2.LEFT JOIN : Displays all customers, including those without orders.

3.Unmatched Orders : Identifies orders that do not have a corresponding customer record.

4.FULL OUTER JOIN Simulation :Displays all matching and non-matching records from both tables.

5.Aggregation : Shows the total number of orders placed by each customer, including customers with zero orders.

